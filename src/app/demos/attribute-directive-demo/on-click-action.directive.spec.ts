import { OnClickActionDirective } from './on-click-action.directive';

describe('OnClickActionDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickActionDirective();
    expect(directive).toBeTruthy();
  });
});
